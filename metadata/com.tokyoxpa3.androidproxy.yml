Categories:
  - Internet
  - Connectivity
License: MIT
AuthorName: tokyoxpa3
SourceCode: https://gitlab.com/tokyoxpa3-group/AndroidProxy
IssueTracker: https://gitlab.com/tokyoxpa3-group/AndroidProxy/-/issues

Name: 5G Proxy Pro
Summary: Lock your phone to 5G/4G and share it as a SOCKS5 proxy over Wi-Fi
Description: |-
  Locks your phone to the cellular (5G/4G) network and exposes a SOCKS5
  proxy server over Wi-Fi, so other devices can route their traffic through
  the phone's cellular connection.

  The proxy engine is a native C implementation (epoll-based) supporting TCP
  (CONNECT) and UDP (ASSOCIATE) with IPv4, IPv6 and domain addresses. All
  connections are bound to the cellular network via Network.bindSocket().
  A heartbeat keeps the cellular link alive, and a health check restarts the
  engine automatically if it fails.

  Optional username/password authentication (RFC 1929) protects the proxy;
  by default the proxy is open, so only use it on trusted local networks.

  A cellular speed test tool is also included.

  Note: Requires a device with an active SIM card and cellular data.

AntiFeatures:
  DisableAlarm:
    en-US: The app requests to be excluded from battery optimisation to keep
      the cellular proxy service running in the background reliably.

RepoType: git
Repo: https://gitlab.com/tokyoxpa3-group/AndroidProxy.git

Builds:
  - versionName: 1.1.0
    versionCode: 3
    commit: v1.1.0
    subdir: app
    gradle:
      - yes
    ndk: 26.3.11579264
    buildjni:
      - yes
    cmake: 3.22.1

AutoUpdateMode: Version
UpdateCheckMode: Tags
CurrentVersion: 1.1.0
CurrentVersionCode: 3
